{
  "name": "foursite-wordpress-promotions",
  "version": "1.8.9.2",
  "private": true,
  "description": "Build tooling for the 4Site WordPress Promotions plugin's hand-written public/admin assets.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
